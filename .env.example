# Copy to .env and fill in real values. .env is gitignored — keep it that way.
# These must also be set in Vercel: Project → Settings → Environment Variables,
# for Production, Preview AND Development.

# Password for the /database alumni table, shared with the whole chapter.
# Make it a long random passphrase, not a word — it is one shared secret
# guarding real people's contact details, and it gets passed around by hand.
# If this is unset, every login is refused and the sheet route returns 503:
# the page fails closed rather than open.
DATABASE_PASSWORD=

# Signs the /database session cookie. Must be random, and must NOT be the
# password. Generate one with:  openssl rand -hex 32
# Changing it logs everyone out. Unset means the page fails closed.
DATABASE_SESSION_SECRET=

# OPTIONAL. The site's absolute origin, used by sitemap.xml, robots.txt and
# metadataBase. On Vercel this is unnecessary — VERCEL_PROJECT_PRODUCTION_URL is
# set automatically and used as the fallback. Set it to force a custom domain in
# preview builds too. With neither set, a production build warns and emits
# localhost URLs.
NEXT_PUBLIC_SITE_URL=

# The long id from the alumni spreadsheet's URL.
GOOGLE_SHEET_ID=

# THE WHOLE service-account JSON, on one line — NOT a path to the file.
# This is the opposite of the usual Google SDK convention and the most common
# setup mistake on this project. Generate with: jq -c . google-service-account.json
GOOGLE_APPLICATION_CREDENTIALS=
